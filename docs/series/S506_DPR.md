# S506 — Rate of Exploitation (e = S*/V*)

**Chapter**: 5 — The Marxian Categories: Empirical Estimates
**Source Table**: Appendix H.1
**Units**: ratio
**Period**: 1948-1989 (book period; extension via BEA NIPA + BLS CES when API access provisioned)
**Content Type**: time_series
**Status**: validated_book_and_extension

---

## Definition

Rate of Exploitation: e = S*/V*. The book's central Marxian magnitude. Identity-checked against S505/S504 ratio.

---

## Subseries

| Subseries | Source | Period | Units |
|---|---|---|---|
| `S506-A` | S&T 1994 | 1948-1989 | ratio |
| `S506-EXT` | BLS CES + BEA NIPA | 1990-2024 | ratio |
| `S506-COMBINED` |  | 1948-2024 | ratio |

---

## Construction

### S506-A (book period, 1948-1989)

1. Read `data/source/book_tables/book_tableH1_1948_1989.csv`.
2. Extract column `S_star_V_star` (and the `year` index).
3. No further transformation — this is the canonical published value.

Validator includes an identity check that verifies the algebraic relation against other already-built series in this chapter.

---

## Reference Values (Validation Benchmarks)

1948=1.70, 1958=2.01, 1967=2.10, 1972=1.99, 1977=2.10, 1989=2.44

Tolerance class: `rate_series` (rel 0.001 / abs 0.01).

Validator: `code/V03_validators/V03_S506_exploitation_rate.py` writes to `data/intermediate/validation/S506.json`.

---

## Provenance Chain

```
data/source/book_tables/book_tableH1_1948_1989.csv
  └── code/L01_loaders/L01_S506_exploitation_rate.py
       └── data/intermediate/S506.csv
            └── code/P02_processors/P02_S506_exploitation_rate.py
                 └── data/final/S506.csv
                      └── code/V03_validators/V03_S506_exploitation_rate.py
```

---

## Citations

Shaikh, Anwar, and E. Ahmet Tonak. 1994. *Measuring the Wealth of Nations: The Political Economy of National Accounts*. Cambridge University Press. Appendix H (Annual Time Series), p. 324-327. Cross-check vs. Appendix E.2 (Revenue Accounts, p. 310).

---

*Generated by anu-ingestion (re-authored from scratch against the Anu Framework v10.0 standard).*

---

## workpackage A REBUILD ADDENDUM (2026-07-01, comprehensive review)
**Extension REBUILT** (headline ROE fix for Prof. Tonak; P1_FORENSIC_REPORT.md + WP-A_REPORT.md).
As-built subseries now: S506-A (book, unchanged) | S506-EXT = consistent BEA operating-surplus
rate (VA_partA-comp)/comp, real SIC 1990-97 + BEA va_components 1998-2024, DISTINCT MEASURE,
break flag DIV-A10 | S506-EXT-MARX = faithful-concept reconstruction (kIO=1.5714 held, DIV-A10)
| S506-EXT-OLD-DEPRECATED = frozen pre-review 1.27 splice-hybrid (retained) | S506-COMBINED =
book + EXT-MARX with methodological-break annotation. Key values: EXT 1997=0.8021/1998=0.7891/
2024=1.1471; MARX 1990=2.4673/1998=2.9707/2024=3.9519. The pre-review 2.44->1.27 cliff was
~100% method (waterfall closes); it is gone. Subseries table above is superseded by this list.
V03: PASS 6/6 book benchmarks + identity. Registry patches queued (WP-A_REGISTRY_PATCHES.json).

> Status upgraded book_period_validated -> validated_book_and_extension on 2026-07-07 (Phase-0 truth baseline, T3): populated extension block + -EXT/-COMBINED subseries + book-arm V03 PASS; parity with the pre-existing validated_book_and_extension series. See registry field `status_adjudication_2026_07_07`.

---

## WP-D1 KIO-RECONSTRUCTION ADDENDUM (2026-07-07)

**The frozen I-O uplift kIO = 1.5714 is replaced, for the COMBINED continuation, by an
officially-sourced time-varying series kIO(t).** New arm **`S506-EXT-MARX-KIO`**: structurally
identical to `S506-EXT-MARX` except the scalar kIO becomes the per-year benchmark-anchored,
bias-corrected `kIO(t)` loaded from `data/source/kio_reconstruction/kio_final_1990_2024.csv`
(promoted source artifact + PROVENANCE; machinery in `Build/KIO_2026-07/{B1,B2,B3,C}`).

**Method (workpackage C "classic splice"):** LEVEL anchored to the native 1992 SIC benchmark
(WP-B1, mid 1.5696, machinery validated backward — kIO₁₉₈₉ reproduced to 0.001%), SHAPE from
WP-B2's NAICS 71-order annual series, real-data SIC bridge 1990–96. Equivalent to the exact
linear rescale `e_kio(t)+1 = (e_marx(t)+1)·kIO(t)/kIO_frozen` (asserted to machine precision;
cross-checked vs `Build/.../e_comparison.csv`). Bias decomposition of the 1.57-vs-1.40 NAICS-era
gap: ~72% omitted royalties, ~23% classification, ~7% aggregation, ~0% genuine change.

**kIO(t):** 1.556 (1990) → 1.5695 (1992) → ≈1.5714 (1996) → 1.731 (2024). The frozen 1.5714 is
**corroborated for ~1990–96 and shown to understate the uplift thereafter** (it is now the
published conservative lower bound, retained unchanged in `S506-EXT-MARX`).

**COMBINED tail repointed** (workpackage C §4.3) from the frozen arm to `S506-EXT-MARX-KIO`. This
**intentionally changes** the published `S506-COMBINED` 1990–2024 values; every changed cell
traces to the new kIO(t) rescale (DIV-028 upgraded / **DIV-071**):

| year | COMBINED before (frozen kIO) | COMBINED after (kIO(t)) |
|---|---:|---:|
| 1990 | 2.4673 | **2.4335** |
| 1998 | 2.9707 | **2.9659** |
| 2024 | 3.9519 | **4.4551** |

**Band + honest caveats.** Uncertainty is published in the sidecar `data/final/S506_KIO_BAND.csv`
(kIO central/lo/hi + e_KIO central/lo/hi). Royalties are applied from the **1992 native** 10%-of-GFP\*
share (not natively read at 71-order); the band **steps wider at the 1996/97 SIC→NAICS seam**
because the two-route perimeter uncertainty is a NAICS-era phenomenon only; 2024 e band [3.64, 4.89].

**V03 anchors (workpackage C §4.6):** 1989 exact (kIO_frozen two-source), 1992 native (±0.5% of 1.5696, in
band), benchmark-year artifact tie (1e-9), e-rescale identity (1e-6). All PASS.